#pragma once

#include <cmph.h>
#include "bench.hpp"

namespace CmphContender {

void benchmark(size_t numKeys) {
  // TODO. Difficulty: cmph does not support integer inputs, only strings.
  //   Should probably convert all to strings, otherwise the benchmark is not fair.
}
};
